[project]
name = "yt-transcript-loader"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=3.1.1",
    "youtube-transcript-api>=1.2.4",
]
